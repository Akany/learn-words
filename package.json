{
  "name": "learn-words",
  "version": "0.0.1",
  "description": "Small app to store words and run a quiz",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev-server": "lerna run --stream --scope server start",
    "dev-client": "lerna run --stream --scope client serve"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.13.1"
  }
}
